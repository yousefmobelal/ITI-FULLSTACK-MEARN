import BlogComponent from '@/components/BlogComponent';
import React from 'react';

const index = () => {
    return (
        <div>
           <BlogComponent/>
        </div>
    );
};

export default index;